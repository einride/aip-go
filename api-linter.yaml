---
# Disabled by default.
- included_paths: ["**"]
  disabled_rules: ["all"]
# Enabled for einride/
- included_paths: ["einride/**"]
  enabled_rules: ["all"]
# Disabled until the following issue has been resolved:
# https://github.com/googleapis/api-linter/issues/597
- included_paths: ["einride/**"]
  disabled_rules: ["core::0124::valid-reference"]
